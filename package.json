{
  "name": "lighthouse-plugin-spa",
  "version": "1.0.0",
  "description": "Lighthouse plugin that reports metrics such as LCP and FCP from a SPA navigation.",
  "type": "module",
  "main": "plugin.js",
  "license": "ISC",
  "scripts": {
    "reset-link": "npm link && npm link lighthouse-plugin-spa",
    "type-check": "tsc",
    "pretest:e2e": "npm run reset-link",
    "test:e2e": "node test/test.js",
    "test": "npm run type-check && npm run test:e2e"
  },
  "devDependencies": {
    "lighthouse": "^10.0.1",
    "puppeteer": "^19.7.1",
    "typescript": "^4.9.5"
  },
  "peerDependencies": {
    "lighthouse": ">=10.0.0"
  }
}
